---
title: クイックスタート
description: "Codex CLI をインストールし、oh-my-codex をインストールした後、`omx setup` を実行し、`omx` でセッションを開始してから `$autopilot` を呼び出します。"
---

Codex CLI をインストールし、oh-my-codex をインストールした後、`omx setup` を実行し、`omx` でセッションを開始してから `$autopilot` を呼び出します。
