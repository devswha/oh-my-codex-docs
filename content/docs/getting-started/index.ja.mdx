---
title: クイックスタート
description: Codex CLI をインストールし、oh-my-codex をインストールした後、`omx setup` を実行し `$autopilot` を呼び出します。
---

Codex CLI をインストールし、oh-my-codex をインストールした後、`omx setup` を実行し `$autopilot` を呼び出します。
